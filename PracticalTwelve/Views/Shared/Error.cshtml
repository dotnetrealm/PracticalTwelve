@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="d-flex justify-content-center ">
    <div class="bg-danger text-white text-center w-50 p-3">
        <h1>@Model.StatusCode</h1>
        <h4>@Model.Message</h4>
    </div>
</div>
